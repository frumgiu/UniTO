 /*{   = x 5;
    = y +(x 2);
    cond
        when(== x y) do print(x)
        when(> x y) do print(1)
        else print(y);

    = c 2
 }
*/
{   = x 5;
    while (> x 0) {
            = x - x 1;
            print(x)
        };
        print(*(1 2 3 - 4 3))
}
